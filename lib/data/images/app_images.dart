/// A class [AppImages] that holds the paths to the image assets used in the application.
///
/// This class contains static constants that represent the file paths
/// to the images stored in the assets directory. These constants can be
/// used throughout the application to reference the images.
class AppImages {
  static const String logo = 'assets/images/smallnews_logo.png';
}
